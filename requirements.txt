# ── Core Django ──────────────────────────────────────────────────────────────
Django==4.2.20
djangorestframework==3.15.2

# ── Authentication ────────────────────────────────────────────────────────────
djangorestframework-simplejwt==5.3.1   # JWT tokens (access + refresh)

# ── Database ──────────────────────────────────────────────────────────────────
psycopg2-binary==2.9.9                 # PostgreSQL adapter

# ── Caching & Task Queue ──────────────────────────────────────────────────────
redis==5.0.3                           # Redis client (OTP storage, rate limiting)
django-redis==5.4.0                    # Django cache backend for Redis
celery==5.3.6                          # Async task queue (PDF generation, notifications)
django-celery-beat==2.6.0             # Periodic tasks scheduler

# ── Security & Encryption ────────────────────────────────────────────────────
cryptography==42.0.5                   # AES-256 / Fernet encryption for sensitive fields

# ── Certificate Generation ───────────────────────────────────────────────────
reportlab==4.1.0                       # PDF generation engine
qrcode[pil]==7.4.2                     # QR code generation for certificates
Pillow==10.3.0                         # Image processing (required by qrcode)
arabic-reshaper==3.0.0                 # Reshapes Arabic characters for correct glyph rendering
python-bidi==0.4.2                     # BiDi algorithm — makes Arabic flow right-to-left in PDFs

# ── File Storage ─────────────────────────────────────────────────────────────
django-storages[s3]==1.14.3            # S3-compatible storage backend
boto3==1.34.69                         # AWS S3 SDK (for PDF/QR storage)

# ── Admin Dashboard ───────────────────────────────────────────────────────────
django-unfold==0.67.0                  # Modern Tailwind admin theme with sidebar + RTL

# ── Utilities ────────────────────────────────────────────────────────────────
python-decouple==3.8                   # Environment variables from .env file
django-cors-headers==4.3.1             # CORS headers for mobile API access
